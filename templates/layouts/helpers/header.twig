<header class="header">
    <div class="container mx-auto">
        {% if theme.theme_mod('custom_logo') %}
            {{ include('components/header/logo.html.twig') }}
        {% else %}
            <h2 class="site-title">{{ fn('get_bloginfo', 'name') }}</h2>
            <h4 class="site-subtitle">{{ fn('get_bloginfo', 'description') }}</h4>
        {% endif %}
    </div>
</header>
